import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Judging' };

// TODO: Judging system overview, accreditation levels, Become a Judge CTA — design handoff pending
export default function JudgingPage() {
  return (
    <div className="mx-auto max-w-7xl px-6 py-16">
      <h1 className="text-3xl font-bold">Judging</h1>
      <p className="mt-4 text-gray-500">[Placeholder — design handoff pending]</p>
    </div>
  );
}
