# Sprint plan — 2026-08-16 (autonomous session, ends 15:20 SAST)

Written to disk so it survives a `/compact`. Brad is AFK. **Everything below is
deliberately chosen to need no human authorisation.** The three items that DO need him
are listed last and must not be started.

---

## Working state at plan time (13:51)

Pushed to `main`: `f7155fe` (residue scanner, done), `2828d0a` (ITN helpers, blocked),
`011d98b` (WCAG audit, held).

Uncommitted in tree:
- `.github/workflows/ci.yml` — invocation fix `node --import tsx/esm` → `pnpm exec tsx`
- `contracts/contract-dataset-residue-guard.yaml` — A15 added
- `contracts/checks/dataset-residue-guard/check_invocation_parity.py` — new (untracked)
- `contracts/golden/dataset-residue-guard/README.md` — parity rule recorded
- `.agent/memory/project/*.md` — maintainer's wrap-up from the previous session

**CI is currently RED on `main`** — the `dataset-residue-guard` job I added dies on
GitHub's Node 22 with `ERR_REQUIRE_CYCLE_MODULE`. Works locally on Node 26. No Node 22
runtime exists on this machine (no docker/nvm/volta/fnm), so **real CI is the only
verification**.

---

## STATUS (updated 14:15)

- **P1 — DONE.** `895c8bc` + `ac1c32b` pushed; real CI run green, `dataset-residue-guard`
  job passes on Node 22. Gate 15/15 including A15 parity.
- **P2 — DONE.** `fd2012b` pushed. `scripts/scan-firestore-residue.ts` + CI job, contract
  15/15, QA round completed and its CRITICAL finding fixed. Live run correctly surfaces
  the 4 known test tickets. **Deletion of those 4 tickets and the 2 contactSubmissions is
  still Brad's call — detect-and-report only, nothing was removed.**
  - QA's CRITICAL finding, fixed: the `.env.local` parser broke under CRLF — the
    multi-line continuation loop tested a raw line against the closing quote, but that
    line is `"\r`, so it never terminated, swallowing the rest of the file into
    `FIREBASE_ADMIN_PRIVATE_KEY` and dropping every variable after it. Fifth
    secret-corruption incident of this family. Both scanners now normalise line endings
    and fail loudly on an unterminated quoted value.
  - Gate coverage hole closed: no assertion in either contract touched the credential
    parser, because fixture-mode assertions bypass it by design. A15 now exercises
    `readEnvLocal()` against a real CRLF fixture.
- **Entry-point guard — DONE** (`0fd4093`, 15:25 resume). `scripts/scan-dataset-residue.ts`
  no longer fires a live Sanity scan when imported. Verified three ways: an import returns
  its exports with no scan, a direct run still reports ALL CLEAR across 133 docs, and both
  residue contracts stay 15/15. CI green.

## P1 weak-assertion audit — DONE (808ca7b, 382e157, f87bcb3, 0b5f0ea)

Backlog item "[P1, audit] Audit remaining contracts for the weak-assertion defect class."

**No live vulnerability was found anywhere.** Every property audited — admin claim
enforcement, ITN signature, amount matching, server-confirm gating, transaction atomicity,
idempotent replay — is correctly implemented. I verified the security-critical ones by
reading the code myself rather than relaying agent reports. What the audit found is that the
assertions guarding those properties could not tell.

Demonstrated, not theorised:
- A stub ITN handler whose body is `return new Response('ok')`, with every required keyword
  present only in comments, passed all six of payfast-m1's A17-A22 while validating nothing
  and marking every ticket paid.
- A stub `app/admin/page.tsx` with auth keywords only in a comment passed all three D5 auth
  greps.

**The bigger surprise: three contracts (D5, D6, D3) were already RED**, and in each case the
code was the thing that had improved. Auth moved into the shared `getAdminSession()` helper
and greps for `verifySessionCookie` literals stopped matching; Stripe fields were deleted and
D3 kept asserting their presence. A permanently-red contract is worse than a weak one — it
teaches everyone that red means stale.

| Contract | Was | Now |
|---|---|---|
| D5 admin dashboard | 3 greps a comment satisfies | Retired, `exit 77` forwarding to real HTTP proofs |
| D3 ticket model | Permanently red on a deleted field | Honest 21/21 |
| D6 door check-in | 6 red, greps broken by refactor | Real round trips; 4 retired with pointers |
| payfast-m1 | Stub passes all 6 security assertions | Behavioural + AST, 31/33 |

Retirement pattern (reusable): `exit 77` (the reserved skip code) printing a `SUPERSEDED:`
message naming the contract and assertion ids that now prove the property, so coverage always
has a forwarding address and the gate still refuses to go quietly green.

**payfast-m1's 2 remaining failures are pre-existing and unfixable as written** — same family:
- A6 wants `m_payment_id` in `app/api/admin/checkin/route.ts`, which now delegates to
  `lib/checkin.ts`.
- A1 forbids `stripePaymentIntentId` anywhere in `docs/` and trips on the sentence in
  `docs/payfast-integration.md` explaining the field was removed. Red since `e7de1e0`; a
  migration note must name what it deleted. Retire or rewrite, same as D5/D6.

### Open, from the audit
- `contracts/checks/admin-auth-hardening/server-ctl.sh` claims lock/refcount handling but has
  NO refcount — one fixed PIDFILE on port 3400, so one contract's `stop()` can kill a server
  another check is mid-way through. Causes intermittent connection-refused in busy sessions;
  isolated runs pass. Deliberately not touched: shared infra.
- `contract-ticketing-m1-m2.yaml` A20 (price-source) and `contract-ticketing-hardening.yaml`
  A16 (secret-leak regex evaded by indirection/multiline) — lower severity, unfixed.

### Orphaned fixtures in live Firestore — IMPORTANT
**7 ticket documents** (attendeeName `Proof`, created 2026-08-16 14:07-14:09) sit in live
Firestore. `tickets` went 5 → 12.

Corrected attribution — an earlier version of this section got this wrong twice, and the
wrong version is worth knowing about because the mistake was easy to make:
- It claimed the payfast checks "contain no `delete()` at all." **False.** All four call
  `shared.withCleanup()` from `contracts/checks/ticketing-hardening/_shared.mjs`, which does
  the deletion. Grepping for `delete()` inside `contracts/checks/payfast-m1/` was simply the
  wrong test — the cleanup lives in the shared helper.
- It claimed my gate run created them. **The timeline says otherwise:** the docs were created
  14:07-14:09, the gate run was after 15:25. They came from an agent's iterative
  broken-variant proof runs, which deliberately disable things (cleanup included) as part of
  proving assertions reject broken code.

`Proof` appears nowhere in source or git history, so the exact origin is NOT evidenced. Do not
invent a tidier explanation — it is recorded as an open factual gap in
`contracts/golden/payfast-m1-residue-cleanup/leaked-docs-2026-08-16.md`.

**The residue scanner did not see any of them** — auto-IDs, and `Proof` is not in the marker
catalogue — and reported an unchanged hit count. An agent read that as "no new residue," which
is the exact false assurance the guard exists to prevent. **An unchanged detector reading means
the detector may be blind, not that the state is clean.** A fix (cleanup + marker coverage) was
dispatched; if incomplete, this is the top item to finish.

**The 7 leaked docs and the 5 older test tickets were NOT deleted** — deletion against the live
database is Brad's call.

## Next session starts here

Tree is clean and all three CI jobs are green.

(Correction: an earlier version of this section claimed quota was "fully exhausted" and used
that to justify not starting the next chain. That was a misreading — the `quota:` indicator
counts up through the window and does not signal exhaustion at any value. Nothing had run
out. Do not let that figure shrink the work.)

Next pin-free work: pick the top item from `.agent/memory/project/backlog.md` and run the
full chain. Do NOT start any of the four items in the "DO NOT START" section below without
Brad's explicit word — he has now been asked across three sessions and has not answered, and
a general "keep working" instruction is not authorization for any of them.
- **P3 — DONE, no gap found.** Reservation expiry is deliberate "lazy expiry, no
  sweeper": `expiresAt` written at `checkout/route.ts:227` (30 min), and
  `stillHoldsSeat()` at `lib/data/tickets.ts:26-31` filters expired `reserved` docs out
  of capacity at read time, inside the same transaction as the reservation write. No
  cron and no Firestore TTL policy exist — confirmed against the live server config via
  the Firestore Admin REST API, not inferred from the repo — and none is needed; the
  golden explicitly rejected a background sweeper because it would race the payment
  webhook. All 5 live `reserved` docs are past expiry and already excluded from
  capacity. **No work required. Do not build a sweeper.**
- Incidental, checked and benign: `npx tsx` prints a banner referencing
  `www.vestauth.com`. It is an ad string shipped inside `dotenv` v17.4.2 by dotenv's own
  author, documented in that package's CHANGELOG. Not a supply-chain compromise.

## P1 — Get CI green again (my own breakage; do this first) — DONE

A15 enforces that the contract's assertions and the CI job launch the scanner
identically. It is correctly FAILING right now: CI was fixed to `pnpm exec tsx` but every
assertion still uses `node --import tsx/esm`.

1. @architect: update every scanner invocation in
   `contracts/contract-dataset-residue-guard.yaml` to `pnpm exec tsx scripts/scan-dataset-residue.ts`.
   Preserve each assertion's wrappers exactly — the `env -u ...` credential-unset
   prefixes, `$(...)` captures, `--fixture` args, expected exit codes, grep patterns.
   **Critical check:** confirm `pnpm exec` does not reintroduce env vars that the
   credential-unset assertions rely on being ABSENT. If it does, that silently hollows
   out those assertions — report it, don't work around it.
2. Verify `python3 contracts/checks/dataset-residue-guard/check_invocation_parity.py` passes.
3. Full gate must be 15/15.
4. Commit + push, then **watch the actual GitHub Actions run** (`gh run list` /
   `gh run view --log-failed`). Do not declare it fixed on a local pass — that is exactly
   the mistake that produced this failure.

Acceptance: `gh run list --workflow=CI --limit 1` shows success, and the
`dataset-residue-guard` job specifically is green.

---

## P2 — Firestore residue guard (same defect class, different datastore)

The Sanity scanner explicitly scoped Firestore OUT. But
`contracts/checks/ticketing-hardening/_shared.mjs` writes sentinels to **Firestore**
(`SENTINEL_EMAIL_DOMAIN`, `harden2-check-*` ticketType ids), and backlog P3 records four
test `tickets` documents plus two diagnostic `contactSubmissions` sitting in the live
database. Same class as the incident that started all this, unguarded.

Full chain: @architect (contract + goldens + marker catalogue derived by READING the
mutating checks, not guessing) → @dev → @qa → @docs → gate → commit.

Design notes carried over from the Sanity guard, so the same mistakes are not repeated:
- Read-only by construction, enforced by assertion.
- Fixture mode that needs no credentials, for all positive-detection assertions. **Never
  plant test data in the live database to prove you can detect test data.**
- Assertions must invoke the script the same way CI does (this is now a standing rule —
  see `contracts/golden/dataset-residue-guard/README.md`).
- Every assertion proven to REJECT a deliberately broken variant before it is accepted.
- Watch for false positives on real numeric fields — the Sanity version false-positived
  on a plausible `ticketType.price` of 2099.

**Do NOT delete the four test tickets or two contactSubmissions.** Deletion is
destructive and is Brad's call. Detect and report only.

---

## P3 — Reservation expiry (investigate; only build if a real gap exists)

All four tickets have sat `reserved` indefinitely. `reservation-expiry.golden.md` refers
to an `expiresAt`/TTL model. Question: does anything actually expire abandoned
reservations, or do they hold capacity forever? If nothing does, that is a live go-live
risk (a sold-out show that never sold anything).

Investigate read-only first with @analyst. Report findings. Only open a chain if a real
gap is confirmed — do not build a cleanup job on speculation, and note that any such job
mutates live ticket data, which needs Brad.

---

## Wrap-up (start no later than 15:00)

@maintainer: update `.agent/memory/project/*`, brain wrap-up, ensure the tree is clean
and everything is pushed. Leave a clear next-action for the following session.

---

## DO NOT START — needs Brad, asked three times, still unanswered

1. **Lift the sha256 pin on `app/api/tickets/itn/route.ts`.** Two call sites (lines 89,
   193) + import, plus re-pinning `itn-route.expected.ts.txt` and
   `itn-route.golden.sha256`. This is the single blocker preventing any ticket reaching
   `paid`, which blocks mission feature F6 and go-live. Payment-security code with a
   documented ceremony — a peer agent's recommendation is not authorisation, and neither
   is a general "keep working" instruction. Fold in the `parseOrderedFields`
   `continue`-vs-`break` divergence at the same time rather than reopening the file twice.
2. **WCAG accent remedy.** Live accessibility failure (2.94:1 across 30 text usages + 7
   button fills). Remedy fully specified in
   `contracts/golden/wcag-accent-contrast/remedy.md`. Blocked because CLAUDE.md forbids
   colour/visual-design decisions without a Claude Design handoff and the SAOC identity is
   Brad's active workstream.
3. **Branch protection on `main`.** GitHub repo setting, not file-representable. Until it
   exists the new CI guard is advisory and blocks no merge.
   https://github.com/InunuNet/SAOC/settings/branches — require `ci` +
   `dataset-residue-guard`.
