import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Firebase App Hosting supports full Next.js SSR
  // output: 'standalone' is set at deploy time via apphosting.yaml
};

export default nextConfig;
