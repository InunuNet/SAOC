# Golden: Phase A — A1 (Next.js sanity) + A2 (ESLint flat config + Prettier)

**Contract:** `execution/contract.yaml` — slug `saoc-phase-a1-a2`, phase 1
**Stack:** Next.js 15.5.x · React 19 · Tailwind v4 · TypeScript strict · pnpm
**Author:** @architect — do NOT implement here; @dev implements against this golden.

---

## Expected end state

### A1 — Lockfile + Next.js sanity check (NO code change expected)

A1 is a verification-only feature. The repo already satisfies it; the gate
confirms the toolchain is healthy. @dev should NOT modify dependency versions
unless an A1 assertion fails.

- [ ] `pnpm build` completes with exit 0 (no type or build errors).
- [ ] `pnpm type-check` (`tsc --noEmit`) completes with exit 0.
- [ ] Installed `next` is `15.x` (currently 15.5.19). `package.json` pins `^15.x`.
- [ ] Installed `react` is `19.x` (currently `^19.0.0`).
- [ ] `pnpm-lock.yaml` is present and committed (already in `.prettierignore`).

### A2 — ESLint flat config + Prettier (ONE file changes)

Single delta: the `package.json` `lint` script. Everything else already exists.

- [ ] **CHANGE — `package.json`:** `"lint": "next lint"` → `"lint": "eslint ."`
      (`next lint` is deprecated since Next.js 15.5.)
- [ ] No other script changes. `format` (`prettier --write .`) and
      `format:check` (`prettier --check .`) stay as-is.
- [ ] `eslint.config.mjs` (flat config, FlatCompat, extends
      `next/core-web-vitals next/typescript prettier`) unchanged — already present.
- [ ] `.prettierrc.json` unchanged — already present.
- [ ] `.prettierignore` unchanged — already includes `*.md` / `*.mdx`
      (see learned.md 2026-06-06; omitting these breaks `format:check`).
- [ ] `pnpm lint` (now `eslint .`) exits 0 with zero errors.
- [ ] `pnpm format:check` exits 0 — repo is already Prettier-clean.

---

## File-change manifest

| File                 | Action  | Detail                                    |
| -------------------- | ------- | ----------------------------------------- |
| `package.json`       | modify  | `lint` script: `next lint` → `eslint .`   |
| `eslint.config.mjs`  | none    | already correct                           |
| `.prettierrc.json`   | none    | already correct                           |
| `.prettierignore`    | none    | already correct (`*.md`, `*.mdx` present) |
| `execution/contract.yaml` | (architect) | written this session              |

**Net diff: exactly one line in `package.json`.**

---

## Assertion → expected result map

| ID  | Check                                            | Pre-fix | Post-fix |
| --- | ------------------------------------------------ | ------- | -------- |
| A1a | `pnpm build` exit 0                              | PASS    | PASS     |
| A1b | `pnpm type-check` exit 0                         | PASS    | PASS     |
| A1c | installed `next` is 15.x                         | PASS    | PASS     |
| A1d | `react` 19 installed                            | PASS    | PASS     |
| A2a | `eslint.config.mjs` exists                       | PASS    | PASS     |
| A2b | lint script uses `eslint` CLI                    | **FAIL**| PASS     |
| A2c | lint script no longer `next lint`                | **FAIL**| PASS     |
| A2d | `pnpm lint` exit 0                               | PASS\*  | PASS     |
| A2e | `pnpm format:check` exit 0                       | PASS    | PASS     |

\* A2d may currently pass via deprecated `next lint`; after the fix it passes
via `eslint .`. The behavioural target is "lint runs clean through eslint".

**Only A2b and A2c fail against current code.** Both are satisfied by the single
`package.json` lint-script edit.

---

## Gate procedure (for @qa / orchestrator)

```
python3 execution/contract.py clear saoc-phase-a1-a2   # clear cached results first
python3 execution/contract.py gate execution/contract.yaml --phase 1 --run-checks
```

Done = all 9 phase-1 assertions green.

## Notes / gotchas

- Negative assertion A2c uses `! grep -q ...` — NOT `grep -vq` (`grep -vq` returns 0
  for any multi-line file and would falsely pass).
- The contract uses the `assertions: {phase, checks}` shorthand; `normalize_contract`
  converts each `command:` to `verify.kind: shell`. Do not hand-author `verify:` blocks.
- Run `contract.py clear` before any re-run; the gate caches prior verdicts.
