# Memory

- Template slug collision fix upstreamed locally (2026-06-16): `template/execution/mission.py::cmd_new()` now has the cross-date slug scan matching live `execution/mission.py` lines 183-205. Prevents regression when `make update-template` copies HARNESS files from template to live. Root cause: fix was applied to live file only; template was never updated. NOTE: `VALID_MISSION_STATUS` in `template/execution/mission.py` line 38 is still missing `"close_out"` — pre-existing lag, track for next template sync (backlog item added).
- `template/.agent/providers/codex.json` created (2026-06-16): `PROVIDER=codex` was documented in `Makefile job` target but manifest was absent. All 8 required keys present. `python3 execution/provider_manifest.py --project-root template validate` now lists codex.
- GitHub Issue #1274 (bug: require_docs.sh hook substring-matches any command containing gate keyword) has been resolved. The `execution/hooks/require_docs.sh` script's `case` statement was updated to use strict prefix matching for "contract.py gate" commands, removing the leading wildcards. This prevents the hook from incorrectly activating on commands that merely contain "gate" as a substring. The change ensures the hook only triggers when the command explicitly starts with `contract.py gate` or is exactly `contract.py gate`.