# Reboot Context
_Generated: 2026-09-10T19:39Z_

## What happened last session
M4 close-out plan on disk (.agent/memory/project/plans/2026-09-11-m4-closeout.md). BLOCKER root-caused: seed writes dotted doc ids (showPage.NN-slug); the dataset's public read grant is _id in path("*") which matches ONE segment, so all 13 showPage docs are invisible to anonymous reads. Fix is hyphens in the seed, NOT a token on the read path (lead vetoed; that would green the routes while shipping a public site that authenticates to read its own public pages). Acceptance test is an anonymous count of 13, not a 200. Design lane sent final rules in prose: R14-R19 + R3 reversal recorded in .agent/memory/project/design/NOS-DESIGN-FINAL-2026-09-10.md. R19 supersedes R11 clause 1 (disclosed extent is a surface, not a dashed stripe) and unblocks the ShowPageProse rebuild. R13/7 caps text-bearing cards at 4 but allows 5-6 for uniform tiles; the hub's grid-cols-5 IS a defect (JudgingGroupCard is text-bearing) and my earlier leave-it-alone call was wrong. Rulings mirror FAILED transfer twice (checksum 707 vs 709; then a safeguard killed the base64 courier) - mirror stays stale and non-authoritative, contract must not be re-cited by rule number until a verified file lands.
