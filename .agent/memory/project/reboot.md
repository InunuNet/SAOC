# Reboot Context
_Generated: 2026-09-01T07:24Z_

## What happened last session
Vendor pipeline complete end-to-end: application -> committee approval -> human-readable code (BusinessName-1234, 5-attempt lockout, generation-bound revocation) -> gated form -> review -> stand payment (ships prices null; source doc deleted its Booth Fees section entirely). Public supporter registration F1 12/12, no auth account. SECURITY: all four HMAC token modules accepted mutated tokens - Buffer.from(str,'hex') truncates silently, so junk appended to a valid signature decoded identically. Fixed with one shared isWellFormedHexDigest. Not exploitable across records, but 21/23 orders carry live recovery tokens and 4 were emailed to real inunu.net addresses. New standing contract-token-canonicality discovers token modules by SHAPE and found the fourth module minutes after being written. Four distinct ways a verdict proved fake this session: gate replaying cache without --run-checks, a chained assertion read one link at a time, 8 checks that never loaded reported as FAIL, and a check that ran and proved nothing (vacuous snapshot equality). Codex caught 6 real defects the gate was green over.
