# ---- Athanor framework (pre-existing) ----
.DS_Store
.tmp/
node_modules/
__pycache__/
.env
.agent/memory/scratch/*
!.agent/memory/scratch/.keep
.agent/memory/brain/
CLAUDE.local.md
GEMINI.local.md
.claude/settings.local.json
*.pyc
.venv/
pulse.log

# ---- Next.js ----
.next/
out/
build/
next-env.d.ts
*.tsbuildinfo

# ---- Local env (never commit) ----
# .env is already ignored above; also ignore any *.local variants
.env*.local
!.env.local.example

# ---- Firebase ----
.firebase/
firebase-debug.log*
firestore-debug.log*
ui-debug.log*
# Emulator data exports
firebase-export-*/

# ---- Cloud Functions build output ----
functions/lib/
functions/node_modules/

# ---- Secrets / keys ----
*.pem
*-firebase-adminsdk-*.json
ops-secrets.local.md
Old SAOC Website Backup/

# ---- Logs ----
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ---- IDE / editor ----
.idea/
.vscode/*
!.vscode/settings.json

# ---- OS ----
Thumbs.db

# ---- Claude Code worktrees (agent artifacts) ----
.claude/worktrees/
!.agent/memory/scratch/improvement_loop/.gitkeep
.agent/memory/scratch/improvement_loop/*
!.agent/memory/scratch/improvement_loop/.gitkeep
.agent/memory/project/failures/*
!.agent/memory/project/failures/.gitkeep
.agent/memory/project/inbox/archive/
.agent/memory/project/inbox/*.txt
.claude/scheduled_tasks.lock
.agent/pulse/registry/processing/*.lock
execution/tests/ghost-project/*/tests/fixtures/*.jsonl.tmp
*.log
fleet_update_report_*.md
*_debug.*
execution/tests/stress_test_log.txt
.vercel
# Secrets (defense in depth — GH #1290)
.secrets/
.env.*
!.env.enc
!.env.example
!.env.sample
!.env.template
!.env.dist
*firebase-adminsdk*
*-adminsdk-*.json
# Key material (SEC-P1-2)
*.key
*.p12
*.pfx
*.p8
*.jks
.agent/overnight-resume.lock

# Lee-Ann Drive mirror — Spec V3 contains plaintext mailbox passwords (Section: Email Collection Checklist)
documents/from-leeann-drive/

# Locally-trusted dev certificates (mkcert) — never commit private keys
.certs/

# Playwright CLI session state (browser QA agents)
.playwright-cli/

# ---- Generated door test-ticket QR sheet (F6) -- regenerated by `pnpm door:seed` ----
scripts/output/

# Run-time generated mutation scratch (contracts/checks/payment-seam-f2/check-reject-path-mutations.sh)
contracts/checks/**/_mutants/

# QA agent scratch (debug scripts, session state, screenshots) — local-only, never committed
.qa_scratch/
# Transient agent worktrees — never commit (they get recorded as bogus gitlinks)
# Transient F5 containment-test bait dir; created and torn down by
# goldens/verify_gate_sweep_isolation.py, must never be committed
.agent/memory/project/specs/repo-wide-gate-sweep/goldens/fixture_specs_f5r2/escape-bait/
