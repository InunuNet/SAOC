- **GitHub Issue #1274 (bug: require_docs.sh hook substring-matches any command containing gate keyword)**: This issue has been resolved *by the agent*. The `execution/hooks/require_docs.sh` script's `case` statement was updated to use strict prefix matching for "contract.py gate" commands, removing the leading wildcards. This prevents the hook from incorrectly activating on commands that merely contain "gate" as a substring. The change ensures the hook only triggers when the command explicitly starts with `contract.py gate` or is exactly `contract.py gate`. The fix was applied and *verified by the agent* by inspecting the code and running `test_require_docs_fix.sh`.