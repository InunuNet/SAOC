- **GitHub Issue #1274 (bug: require_docs.sh hook substring-matches any command containing gate keyword)**: This issue has been resolved and re-verified. The previous record of its resolution was inaccurate. The `execution/hooks/require_docs.sh` script's `case` statement was updated to use strict prefix matching for "contract.py gate" commands, changing `*"contract.py gate "*|*"contract.py gate")` to `"contract.py gate "*|"contract.py gate")`. This prevents the hook from incorrectly activating on commands that merely contain "gate" as a substring. The change ensures the hook only triggers when the command explicitly starts with `contract.py gate` or is exactly `contract.py gate`. The fix was applied and re-verified by inspecting the code, as direct execution of `test_require_docs_fix.sh` was prevented by policy.