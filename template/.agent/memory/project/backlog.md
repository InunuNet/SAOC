# Backlog

The mission queue. Items use `- [ ]` / `- [x]` checkbox format — the maintainer's
tick step matches nothing else.

## TODO

<!-- No items yet. Run /onboard to define the project goal and tech stack, then
     add the work it implies here. -->
